<Window x:Class="PTwoManage.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="P2Manage" Height="350" Width="525"  WindowState="Maximized"  
        >
    <DockPanel>
        <StackPanel Name="loginPanel" Margin="20" IsEnabled="True" Visibility="Visible" HorizontalAlignment="Right">
            <TextBlock Text="Company Name"/>
            <TextBox Name="loginUsernameBox"/>
            <TextBlock Text="Company Password"/>
            <PasswordBox Name="loginPasswordBox"/>
            <Button Name="loginSubmit" Height="30" Width="100" Content="Log in" Margin="0,10,0,0" Click="loginSubmit_Click"/>
            <TextBlock Text="" Name="loginFeedbackText"/>
            
        </StackPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_Open"></MenuItem>
            </MenuItem>
            <MenuItem Header="_User" Click="AddUser_Click"></MenuItem>
            <MenuItem Header="_Schedule" Click="MenuItem_Click_1">
                <MenuItem Header="_Add Shift Template" Click="AddTemplate_Click"></MenuItem>
                <MenuItem Header="_Load Schedule" Click="LoadSchedule_Click"></MenuItem>
                <MenuItem Header="_Free request" Click="UserFreeRequest_Click"></MenuItem>
            </MenuItem>
        </Menu>

        <StackPanel>
            <!-- Week display -->
            <StackPanel Orientation="Horizontal">
                
                <ListView Margin="10" Name="shiftDataBindingMonday" Width="auto">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <WrapPanel>
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding EmployeeName}" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Day}" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding PrintableStartTime}" />
                                        <TextBlock Text=" - " />
                                        <TextBlock Text="{Binding PrintableEndTime}" />
                                    </StackPanel>
                                </StackPanel>
                            </WrapPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <ListView Margin="10" Name="shiftDataBindingTuesday" Width="auto">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <WrapPanel>
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding EmployeeName}" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Day}" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding PrintableStartTime}" />
                                        <TextBlock Text=" - " />
                                        <TextBlock Text="{Binding PrintableEndTime}" />
                                    </StackPanel>
                                </StackPanel>
                            </WrapPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <ListView Margin="10" Name="shiftDataBindingWednesday" Width="auto">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <WrapPanel>
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding EmployeeName}" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Day}" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding PrintableStartTime}" />
                                        <TextBlock Text=" - " />
                                        <TextBlock Text="{Binding PrintableEndTime}" />
                                    </StackPanel>
                                </StackPanel>
                            </WrapPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <ListView Margin="10" Name="shiftDataBindingThursday" Width="auto">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <WrapPanel>
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding EmployeeName}" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Day}" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding PrintableStartTime}" />
                                        <TextBlock Text=" - " />
                                        <TextBlock Text="{Binding PrintableEndTime}" />
                                    </StackPanel>
                                </StackPanel>
                            </WrapPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <ListView Margin="10" Name="shiftDataBindingFriday" Width="auto">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <WrapPanel>
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding EmployeeName}" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Day}" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding PrintableStartTime}" />
                                        <TextBlock Text=" - " />
                                        <TextBlock Text="{Binding PrintableEndTime}" />
                                    </StackPanel>
                                </StackPanel>
                            </WrapPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <ListView Margin="10" Name="shiftDataBindingSaturday" Width="auto">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <WrapPanel>
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding EmployeeName}" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Day}" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding PrintableStartTime}" />
                                        <TextBlock Text=" - " />
                                        <TextBlock Text="{Binding PrintableEndTime}" />
                                    </StackPanel>
                                </StackPanel>
                            </WrapPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <ListView Margin="10" Name="shiftDataBindingSunday" Width="auto">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <WrapPanel>
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding EmployeeName}" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Day}" />
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding PrintableStartTime}" />
                                        <TextBlock Text=" - " />
                                        <TextBlock Text="{Binding PrintableEndTime}" />
                                    </StackPanel>
                                </StackPanel>
                            </WrapPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
            <StackPanel>
                <TextBlock Text="Info here"/>
            </StackPanel>
        </StackPanel>
    </DockPanel>
    <!--<Grid/> Den var der til at starte med -->
</Window>
